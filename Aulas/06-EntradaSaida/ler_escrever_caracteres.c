#include <stdio.h>

// ler caracteres
// getc
// getchar

// escrever caracteres
// putc
// putchar

int main(){

    char ch = getc(stdin); //entrada padrao = teclado
    putc(ch, stdout); //saida padrao = tela\console

    return 0;
}
